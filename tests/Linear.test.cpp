// TODO: Test disabled because code is still in progress.

#include "regression/Linear.h"
#include <gtest/gtest.h>
#include <iostream>
#include <stdexcept>
#include <vector>

TEST(LinearTest, predict) {
  //LinearModel lr = LinearModel();
  //Matrix obs{3, 1};
  //obs.set_value(0, 0, 10);
  //obs.set_value(1, 0, 20);
  //obs.set_value(2, 0, 30);
  //float pred = lr.predict(obs);
  //float actl = 10;
  // EXPECT_EQ(actl, pred);
}

TEST(LinearTest, _calculate_loss) {
  //LinearModel lr1 = LinearModel();
  //std::vector<float> test1 = {0.0, 1.1, 2.3123123};
  // EXPECT_EQ(10.0f, lr._calculate_loss(test1));
}
